{
  "name": "monitor-bancos",
  "version": "1.0.0",
  "description": "Monitor de Bancos application",
  "main": "server.ts",
  "scripts": {
    "start": "deno run --allow-net --allow-write --allow-read --allow-import server.ts",
    "dev": "deno run --allow-net --allow-write --allow-read --allow-import --watch server.ts"
  },
  "dependencies": {
    "oak": "^12.6.1"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
} 